{
  "slug": "11-PERFORMANCE-OPTIMIZATION-FINDINGS",
  "projectId": "parquet-sourcegenerator",
  "title": "11 - Performance Optimization Findings & Baseline Analysis",
  "category": "Internals & Engineering Spikes",
  "order": 11,
  "headings": [
    {
      "id": "-1-executive-summary",
      "text": "🎯 1. Executive Summary",
      "level": 2
    },
    {
      "id": "-2-empirical-baseline-data",
      "text": "🔬 2. Empirical Baseline Data",
      "level": 2
    },
    {
      "id": "21-il-interrogation-results-scriptsinterrogateilcs",
      "text": "2.1 IL Interrogation Results (scripts/InterrogateIL.cs)",
      "level": 3
    },
    {
      "id": "22-disassembly-analysis-of-string-column-boxing",
      "text": "2.2 Disassembly Analysis of String Column Boxing",
      "level": 3
    },
    {
      "id": "-3-experimental-mechanisms-evaluated",
      "text": "🧪 3. Experimental Mechanisms Evaluated",
      "level": 2
    },
    {
      "id": "mechanism-a-direct-readonlymemoryreadonlymemorychar-writing-recommended",
      "text": "Mechanism A: Direct ReadOnlyMemory<ReadOnlyMemory<char>?> Writing (Recommended)",
      "level": 3
    },
    {
      "id": "mechanism-b-zero-boxing-array-reference-fast-path",
      "text": "Mechanism B: Zero-Boxing Array Reference Fast-Path",
      "level": 3
    },
    {
      "id": "mechanism-c-l1-string-deduplication-cache-analysis",
      "text": "Mechanism C: L1 String Deduplication Cache Analysis",
      "level": 3
    },
    {
      "id": "-4-summary-actionable-recommendations",
      "text": "📋 4. Summary & Actionable Recommendations",
      "level": 2
    },
    {
      "id": "-5-verified-post-optimization-results-issue-116",
      "text": "🏆 5. Verified Post-Optimization Results (Issue 116)",
      "level": 2
    },
    {
      "id": "51-post-optimization-il-interrogation-scriptsinterrogateilcs---check",
      "text": "5.1 Post-Optimization IL Interrogation (scripts/InterrogateIL.cs --check)",
      "level": 3
    },
    {
      "id": "52-verification-suite",
      "text": "5.2 Verification Suite",
      "level": 3
    },
    {
      "id": "-6-branchless-null-bitmap-construction-issue-145",
      "text": "⚡ 6. Branchless Null Bitmap Construction (Issue 145)",
      "level": 2
    },
    {
      "id": "61-what-changed",
      "text": "6.1 What Changed",
      "level": 3
    },
    {
      "id": "62-verification-of-branch-elimination",
      "text": "6.2 Verification of Branch Elimination",
      "level": 3
    },
    {
      "id": "63-measured-results",
      "text": "6.3 Measured Results",
      "level": 3
    },
    {
      "id": "64-negative-result-simd-packing-does-not-pay-here",
      "text": "6.4 Negative Result: SIMD Packing Does Not Pay Here",
      "level": 3
    }
  ],
  "rawMarkdown": "# 11 - Performance Optimization Findings & Baseline Analysis\n\nThis document records the empirical performance baseline of code generated by **Parquet.SourceGenerator**, established using the newly introduced diagnostics tooling:\n- **IL Interrogation Runner** (`scripts/InterrogateIL.cs` utilizing `ilspycmd` and `dotnet-inspect`).\n- **Managed Memory & GC Triage Runner** (`scripts/TriageMemoryDump.cs` utilizing `dotnet-dump` and SOS commands).\n\n---\n\n## 🎯 1. Executive Summary\n\nUsing automated IL disassembly and live memory dump inspection across test workloads and benchmark suites ([`Parquet.SourceGenerator.CLI`](../test/Parquet.SourceGenerator.CLI) and [`Parquet.SourceGenerator.Benchmarks`](../benchmarks/Parquet.SourceGenerator.Benchmarks)), we identified:\n\n1. **String Column Serialization Boxing**:\n   - **Finding**: Every string and byte array column write path in generated row-group serializers emits a `box valuetype System.ArraySegment<T>` opcode.\n   - **Impact**: In analytical models with numerous string columns (e.g. `BenchmarkAdultCensus` with 9 string fields), each row-group write incurs **9 distinct heap boxing allocations** plus interface dispatch overhead.\n   - **Solution Evaluated**: Direct invocation of Parquet.Net's generic `WriteAsync<ReadOnlyMemory<char>>` or non-boxing array reference casting, eliminating 100% of these allocations and bypassing secondary internal `ArrayPool` rentals.\n\n2. **Managed Heap Topography & String Dominance**:\n   - **Finding**: SOS memory dumps (`dumpheap -stat`) during analytical batch processing confirm that `System.String` constitutes the vast majority of live heap allocations (over 3.47 MB across 27,800+ objects).\n   - **Validation**: Confirms that the direct-mapped L1 string deduplicator cache (`StringDeduplicator`) is an essential architectural feature that must be preserved and expanded to all deserialization entry points.\n\n3. **Zero-Boxing for Primitives and Blittable Structs**:\n   - **Finding**: Primitive numeric, timestamp, GUID, and blittable struct serialization pipelines exhibit **0 boxing operations** (`box`), validating that `MemoryMarshal.Cast`, span slicing, and SIMD hardware intrinsics generate tight, zero-allocation native loops.\n\n---\n\n## 🔬 2. Empirical Baseline Data\n\n### 2.1 IL Interrogation Results (`scripts/InterrogateIL.cs`)\n\nRunning the IL interrogation against `Parquet.SourceGenerator.Benchmarks.dll` yielded the following metrics across generated serializers:\n\n| Generated Extension Type | `box` Opcodes | `callvirt` Calls | `newobj`/`newarr` | Status |\n| :--- | :---: | :---: | :---: | :---: |\n| `BenchmarkAdultCensusParquetExtensions` | **9** | 672 | 203 | ⚠️ String Boxing |\n| `BenchmarkTpchLineItemParquetExtensions` | **5** | 705 | 209 | ⚠️ String Boxing |\n| `BenchmarkDiamondsParquetExtensions` | **0** | 490 | 152 | ✅ Zero Boxing |\n| `ScaleEventParquetExtensions` | **0** | 292 | 98 | ✅ Zero Boxing |\n| `GuidEventParquetExtensions` | **0** | 259 | 88 | ✅ Zero Boxing |\n\n### 2.2 Disassembly Analysis of String Column Boxing\n\nExamining the emitted IL for `BenchmarkAdultCensusParquetExtensions.WriteParquetRowGroupAsync` at offset `IL_0692`–`IL_06a6`:\n\n```cil\n// Instantiating the ArraySegment struct\nIL_0692: newobj instance void valuetype [System.Runtime]System.ArraySegment`1<string>::.ctor(!0[], int32, int32)\n\n// BOXING: Boxing the value-type ArraySegment to pass to IReadOnlyCollection<string>\nIL_0697: box valuetype [System.Runtime]System.ArraySegment`1<string>\n\n// Passing the boxed interface reference to ParquetRowGroupWriter\nIL_06a6: callvirt instance class [System.Runtime]System.Threading.Tasks.Task [Parquet]Parquet.ParquetRowGroupWriter::WriteAsync(\n             class [Parquet]Parquet.Schema.DataField, \n             class [System.Runtime]System.Collections.Generic.IReadOnlyCollection`1<string>, \n             valuetype [System.Runtime]System.Nullable`1<valuetype [System.Runtime]System.ReadOnlyMemory`1<int32>>)\n```\n\n#### Why Does This Occur?\n1. In `CodeEmitter.cs`, string column serialization emits:\n   ```csharp\n   await groupWriter.WriteAsync(field, new ArraySegment<string?>(buffer, 0, count));\n   ```\n2. `ParquetRowGroupWriter.WriteAsync` has an overload signature:\n   ```csharp\n   public Task WriteAsync(DataField field, IReadOnlyCollection<string?> values, ...)\n   ```\n3. Because `ArraySegment<T>` is a C# `struct` that implements `IReadOnlyCollection<T>`, passing it as an interface parameter forces the CLR to allocate a boxed heap wrapper on every invocation.\n4. Furthermore, Parquet.Net's internal implementation of `WriteAsync(DataField, IReadOnlyCollection<string?>)` executes:\n   ```csharp\n   ReadOnlyMemory<char>?[] rented = ArrayPool<ReadOnlyMemory<char>?>.Shared.Rent(values.Count);\n   try {\n       int length = 0;\n       foreach (string value in values) { // Virtual interface dispatch + boxing enumerator\n           rented[length++] = value.AsNullableReadOnlyMemory();\n       }\n       await WriteAsync(field, new ReadOnlyMemory<ReadOnlyMemory<char>?>(rented, 0, length), repetitionLevels);\n   } finally {\n       ArrayPool<ReadOnlyMemory<char>?>.Shared.Return(rented);\n   }\n   ```\n   This incurs an extra `ArrayPool` rental and element-by-element iteration inside the underlying library!\n\n---\n\n## 🧪 3. Experimental Mechanisms Evaluated\n\n### Mechanism A: Direct `ReadOnlyMemory<ReadOnlyMemory<char>?>` Writing (Recommended)\n\nSince `ReadOnlyMemory<char>` is a struct, it satisfies the generic constraint `where T : struct` on Parquet.Net's low-level overload:\n```csharp\npublic Task WriteAsync<T>(DataField field, ReadOnlyMemory<T?> values, ...) where T : struct\n```\n\n**Experiment**:\nRather than renting `string?[]` buffers and passing `ArraySegment<string?>` to `WriteAsync`, the generator can:\n1. Rent buffers of `ReadOnlyMemory<char>?` directly from `ArrayPool<ReadOnlyMemory<char>?>.Shared`.\n2. Populate slots using `item.Property.AsMemory()` during row materialization.\n3. Pass `new ReadOnlyMemory<ReadOnlyMemory<char>?>(buffer, 0, count)` directly to `WriteAsync<ReadOnlyMemory<char>>`.\n\n**Observed Benefits**:\n- **0 Boxing**: Completely eliminates the `box valuetype ArraySegment<string>` instruction.\n- **Bypasses Library Rental**: Skips Parquet.Net's internal `ArrayPool.Rent` and secondary string-to-memory loop.\n- **Direct Native Handoff**: Memory segments are handed straight to `DataColumnWriter`.\n\n---\n\n### Mechanism B: Zero-Boxing Array Reference Fast-Path\n\nIf string array buffers remain `string?[]`, we evaluated a hybrid reference cast:\n\n```csharp\nawait groupWriter.WriteAsync(\n    field, \n    count == buffer.Length \n        ? (global::System.Collections.Generic.IReadOnlyCollection<string?>)buffer \n        : new global::System.ArraySegment<string?>(buffer, 0, count));\n```\n\n**Analysis**:\n- When `count == buffer.Length` (full batch / exact row group), `string?[]` is passed directly.\n- Because `string?[]` is a reference type (class), casting it to `IReadOnlyCollection<string?>` is an interface cast (`castclass`), **not a boxing operation**.\n- However, when `count < buffer.Length` (the remainder chunk of an uneven dataset), the `ArraySegment` fallback still triggers boxing. Therefore, Mechanism A remains architecturally superior.\n\n---\n\n### Mechanism C: L1 String Deduplication Cache Analysis\n\nUsing `TriageMemoryDump.cs` with `dumpheap -stat` on `adult_census_income` (32,561 rows with 9 categorical string columns):\n\n```text\nStatistics:\nMethodTable     Count    TotalSize Class Name\n000121a289f0   27,832    3,475,960 System.String\n```\n\n**Evaluation**:\n1. Without deduplication, 32,561 rows × 9 columns produces up to **293,049 string allocations**.\n2. With the direct-mapped L1 cache (`capacity = 512`), repeated categorical strings (e.g. `\"Private\"`, `\"Self-emp-not-inc\"`, `\"United-States\"`, `\"<=50K\"`) hit the L1 cache slot with zero allocations.\n3. This reduces total heap memory from ~24 MB down to ~3.5 MB—an **85% memory reduction**.\n\n---\n\n## 📋 4. Summary & Actionable Recommendations\n\n| Priority | Area | Optimization | Status | Impact |\n| :---: | :--- | :--- | :---: | :--- |\n| **P0** | **Serialization** | Switch string & binary serialization to pooled `ReadOnlyMemory<T>?` buffers and call low-level `WriteAsync<ReadOnlyMemory<T>>`. | ✅ **Completed** (#116) | Eliminates 100% of string and binary serialization boxing (`box ArraySegment<T>`) and removes secondary `ArrayPool` rentals. |\n| **P1** | **Deserialization** | Ensure `StringDeduplicator` is enabled by default for all categorical string columns across streaming and parallel reader paths. | ⏳ Planned | Up to 85% memory savings on analytical datasets. |\n| **P2** | **Diagnostics CI** | Integrate `dotnet run scripts/InterrogateIL.cs --check` into CI to automatically fail builds if new boxing regressions are introduced into generated code. | ⏳ Planned | Guaranteed regression prevention. |\n\n---\n\n## 🏆 5. Verified Post-Optimization Results (Issue #116)\n\nFollowing implementation of Mechanism A in `BufferPoolComponent.cs`, `PropertyMappingComponent.cs`, and `CodeEmitter.cs`:\n\n### 5.1 Post-Optimization IL Interrogation (`scripts/InterrogateIL.cs --check`)\n\n| Generated Extension Type | Baseline `box` | Optimized `box` | Reduction | Status |\n| :--- | :---: | :---: | :---: | :---: |\n| `BenchmarkAdultCensusParquetExtensions` | 9 | **0** | **-100%** | ✅ Verified Zero-Boxing |\n| `BenchmarkTpchLineItemParquetExtensions` | 5 | **0** | **-100%** | ✅ Verified Zero-Boxing |\n| `ProfileEventParquetExtensions` | 1 | **0** | **-100%** | ✅ Verified Zero-Boxing |\n| `BenchmarkDiamondsParquetExtensions` | 0 | **0** | 0 | ✅ Zero Boxing |\n| `ScaleEventParquetExtensions` | 0 | **0** | 0 | ✅ Zero Boxing |\n| `GuidEventParquetExtensions` | 0 | **0** | 0 | ✅ Zero Boxing |\n| All Stream Async State Machines (`<ReadParquetStreamAsync>d__*`) | 14 | **0** | **-100%** | ✅ Zero Boxing |\n\n### 5.2 Verification Suite\n- **Unit & Regression Suite**: 306/306 tests passing including new dedicated `ZeroBoxingSerializationTests.cs`.\n- **Golden File Parity**: Updated and validated in `OrderEventParquetExtensions.g.cs`.\n- **Native AOT Compliance**: Passed full 11-step matrix under native binary compilation with zero trimming regressions.\n\n\n---\n\n## ⚡ 6. Branchless Null Bitmap Construction (Issue #145)\n\n### 6.1 What Changed\n\nGenerated nullable-value-column extraction used to test `Nullable<T>.HasValue` per row and take one\nof two arms. That conditional is data-dependent and, on a semi-sparse column, unpredictable — the\nworst possible input for a branch predictor. It is now branch-free in all three emitted extraction\nshapes (the `List<T>` span loop, the `T[]` span loop, and the `IEnumerable` fallback):\n\n```csharp\nvar val_2 = item.NullableFlag;\nbool has_2 = val_2.HasValue;\nint hv_2 = Unsafe.As<bool, byte>(ref has_2);\nUnsafe.Add(ref dstRef_2, nonNullCount_2) = val_2.GetValueOrDefault();\nnonNullCount_2 += hv_2;\nUnsafe.Add(ref defRef_2, i) = hv_2;\n```\n\nThe payload store is unconditional. A null row writes a discardable `default(T)` at the compaction\ncursor, which the next non-null row overwrites, and the cursor never leaves the buffer because\n`nonNullCount <= i < count <= buffer.Length` (buffers are rented at `count`). Every payload\nconversion was restated in a total form over `default(T)` so evaluating it for a null slot is legal.\n\nThe netstandard2.0 fallback loops use `val.HasValue ? 1 : 0` instead of `Unsafe.As`, since\n`System.Runtime.CompilerServices.Unsafe` is not guaranteed to be referenced by a consumer project\ntargeting netstandard2.0. RyuJIT lowers both to a flag-materialising `cset`.\n\n### 6.2 Verification of Branch Elimination\n\n`BranchlessDefinitionLevelIlTests` walks the IL of the generated `ExtractSpan`/`ExtractArray` local\nfunctions and counts opcodes with `FlowControl.Cond_Branch`. A model with six nullable columns\ncontains **exactly one** conditional branch — the `for` loop's own control edge — and the count is\nidentical to a model with a single nullable column, so no column contributes a branch of its own.\n\n### 6.3 Measured Results\n\nApple M1 (8 cores), macOS 15.7.7, .NET SDK 9.0.315, host .NET 9.0.17 Arm64 RyuJIT AdvSIMD,\nBenchmarkDotNet 0.14.0.\n\n**Extraction microbenchmark** — 100,000 rows, one nullable `int` column, definition levels plus\npacked values, no Parquet I/O, allocation-free. `Aos_*` is the array-of-structures loop the\ngenerator emits; `Columnar_*` is the same work over a contiguous `int?[]`.\n\n| Null % | Aos_Branchy (before) | Aos_Branchless (after) | Speedup | Columnar_TwoPassSimd |\n| ---: | ---: | ---: | ---: | ---: |\n| 0 | 70.63 µs | 69.85 µs | 1.01× | 79.16 µs |\n| 10 | 132.62 µs | 69.01 µs | **1.92×** | 137.30 µs |\n| 20 | 207.81 µs | 69.36 µs | **3.00×** | 136.00 µs |\n| 50 | 437.07 µs | 68.26 µs | **6.40×** | 136.05 µs |\n| 90 | 167.93 µs | 66.79 µs | **2.51×** | 135.44 µs |\n| 100 | 96.91 µs | 66.00 µs | 1.47× | 73.78 µs |\n\nThe branchless loop's cost is flat at ~67-70 µs across every null density, because there is no\nlonger anything for the predictor to get wrong. The branchy loop peaks at 50% null, exactly where\nmispredictions peak.\n\n**End-to-end write** — 200,000 rows, four nullable columns, `WriteParquetAsync` to a `MemoryStream`.\n\n| GC mode | Null % | Before | After | Delta |\n| --- | ---: | ---: | ---: | ---: |\n| Workstation | 20 | 8.388 ms | 8.374 ms | −0.2% (noise) |\n| Workstation | 50 | 9.287 ms | 8.983 ms | −3.3% |\n| Server | 20 | 8.824 ms | 8.615 ms | −2.4% |\n| Server | 50 | 9.561 ms | 9.025 ms | −5.6% |\n\nAllocation is unchanged (5.36 MB/op in every configuration). End to end, Parquet.Net's encoding and\ncompression dominate, so a 6× win in extraction shows up as a few percent of wall clock. Both wins\nare real; they are just measuring different denominators.\n\n### 6.4 Negative Result: SIMD Packing Does Not Pay Here\n\nIssue #145's second proposal — load vectors of `Nullable<T>`, extract null masks into SIMD vectors,\n`PopCount` the mask — was implemented as `NullableColumnExtractor.ExtractTwoPass` (a branchless\nsplit into a presence bitmap plus unpacked payloads, then a vectorised widen of the bitmap into\ndefinition levels and a vectorised in-place compaction) and **measured slower than the branchless\nscalar single pass at every null density** (see the table above: ~136 µs vs ~68 µs on mixed data).\n\nTwo reasons, both structural rather than incidental:\n\n1. **The write path's source is an array of structures.** Extraction reads `item.Property` across a\n   span of row objects; the `Nullable<T>` slots are strided by the row size and are not vector\n   loadable at all. Any vector form must first gather them into a contiguous staging buffer, and\n   that extra pass over 100k elements costs more than the branch it removes.\n2. **Arm64 has no vector compress.** The value compaction is a stream compaction. AVX-512 has\n   `VPCOMPRESS`; NEON does not, so the mixed-block case falls back to the same element-at-a-time\n   branchless store the scalar path already does, and only the all-present / all-absent 16-byte\n   blocks take a vector fast path.\n\n`NullableColumnExtractor` is kept as a tested public helper because the columnar hand-off work\n(#136, #137) will produce contiguous `Nullable<T>` columns, where the picture may differ — but on\nthis shape and this hardware, **the branchless scalar single pass is the right answer and the\nvectorised one is not**.\n",
  "readingMinutes": 10
}